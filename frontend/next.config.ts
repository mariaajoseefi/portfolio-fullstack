/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",

  // basePath: "/portfoliofullstack",
  // assetPrefix: "/portfoliofullstack",

  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;

